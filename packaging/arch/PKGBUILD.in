pkgname=cockpit-session-recording
pkgver=VERSION
pkgrel=1
pkgdesc='Cockpit Session Recording Module'
arch=('x86_64')
url='https://github.com/Scribery/cockpit-session-recording'
license=(LGPL)
source=("SOURCE")
sha256sums=('SKIP')

package() {
  depends=(cockpit)
  cd $pkgname
  make DESTDIR="$pkgdir" install PREFIX=/usr
}
